meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: false
run:
  persistenceMount: /data
  scriptName: bot.py
  containerPort: 80
  servicePort: 80